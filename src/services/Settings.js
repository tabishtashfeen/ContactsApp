// import moment from 'moment';

export const settings = {
  baseUrl: '', // TEST
  user_login_details: 'USER_LOGIN_DETAILS',
  userDetails: 'USER_DETAILS',
};

// export const compareTwoDates = (startDate, endDate) => {
//   const date1 = moment.utc(startDate).format('YYYY-MM-DD HH:mm');
//   const date2 = moment.utc(endDate).format('YYYY-MM-DD HH:mm');

//   // and i checked with if statement
//   if (date1 > date2) {
//     return 'gt';
//   }
//   if (date1 === date2) {
//     return 'eq';
//   }
//   return 'lt';
// };
